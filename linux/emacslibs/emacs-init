(defun eshell/clr ()  (interactive)  ( let ((inhibit-read-only t))(erase-buffer)))(put 'erase-buffer 'disabled nil)(custom-set-variables ;; custom-set-variables was added by Custom. ;; If you edit it by hand, you could mess it up, so be careful. ;; Your init file should contain only one such instance. ;; If there is more than one, they won't work right. '(cua-mode t nil (cua-base)) '(inhibit-startup-screen t) '(show-paren-mode t) '(text-mode-hook (quote (turn-on-auto-fill text-mode-hook-identify))))(custom-set-faces ;; custom-set-faces was added by Custom. ;; If you edit it by hand, you could mess it up, so be careful. ;; Your init file should contain only one such instance. ;; If there is more than one, they won't work right. '(default ((t (:family "Cousine" :foundry "GOOG" :slant normal :weight normal :height 108 :width normal)))))(setq-default fill-column 150)(add-to-list 'load-path "~/bin/emacslibs")(load-library "window-number")(load-library "bm")( setenv "JAVA_HOME" "/usr/local/share/jdk/latest" );; Set key settings for bookmarks(global-set-key (kbd "<f2>") 'bm-toggle)(global-set-key (kbd "<C-f2>")   'bm-next)(global-set-key (kbd "<S-f2>") 'bm-previous)(setq x-select-enable-clipboard t);;(setq interprogram-paste-function 'x-cut-buffer-or-selection-value)(setq interprogram-paste-function 'x-selection-value)(set 'make-backup-files nil);; color management(set-background-color "gray90");;(set-foreground-color "goldenrod2")(window-number-meta-mode 1)(window-number-mode 1)( setenv "PATH" "/home/kuttybanerjee/bin/nim-2.2.10/bin:/home/kuttybanerjee/.juliaup/bin:$JAVA_HOME/bin:/home/kuttybanerjee/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/snap/bin" )( setenv "PATH" ( concat ( getenv "PATH" ) ":~/bin:/home/kuttybanerjee/.cargo/bin" ) );; That's the logging library we use and this is the ( setenv "SPDLOG_LEVEL" "critical" );; Perforce settings( setenv "P4PORT" "localhost:12002" )( setenv "P4USER" "kbanerjee" )( setenv "P4CLIENT" "one" )( setenv "P4EDITOR" "emacsclient" )( setenv "P4ROOT" "/home/kutty/daji/code/perforce/one" );; Emacs / git / shell etc( setenv "EDITOR" "emacsclient" )( setenv "TERM" "eterm" )( setenv "PAGER" "more" ) ;; This will allow git and eshell to work well;; if however you do git lg -200 and more is annoying, replace it with cat and you will get all 200 of them displayed;; at once(server-start)(eshell)